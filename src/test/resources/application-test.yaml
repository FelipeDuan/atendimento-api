spring:
  datasource:
    username: atendimento_app
    password: atendimento
  flyway:
    user: test
    password: test

# logging:
#   level:
#     org.hibernate.SQL: debug
#     org.hibernate.orm.jdbc.bind: trace
